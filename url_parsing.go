package main

/*import (
	"fmt"
	"net"
	"net/url"
)
func main(){
	p:=fmt.Println
	db_url:="postgres://user:pass@host.com:5432/path?k=v#f"
	u,err:=url.Parse(db_url)
	if err!=nil{
		panic(err)
	}
	p(u.User)
	p(u.User.Username())
	p(u.User.Password())
	p(u.Fragment)
	p(u.Hostname())
	_,port,_:=net.SplitHostPort(u.Host)
	p(port)
	p(u.Path)
	p(u.RawQuery)
	m,_:=url.ParseQuery(u.RawQuery)

	p(m)
	p(m["k"][0])

}
	*/
	